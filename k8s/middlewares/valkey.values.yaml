# =============================================================================
# Alephant — Valkey (Redis 兼容) 部署配置
# =============================================================================
# Chart: valkey/valkey (官方 Chart)
# 用法:
#   helm repo add valkey https://valkey.io/valkey-helm/
#   helm upgrade --install alephant-valkey valkey/valkey \
#     --version 0.9.4 \
#     --namespace alephant-prod \
#     --create-namespace \
#     -f k8s/middlewares/valkey.values.yaml
# =============================================================================

image:
  tag: 9.0.2

replica:
  enabled: true
  replicas: 0
  persistence:
    size: 2Gi
    storageClass: local-path

dataStorage:
  enabled: true
  requestedSize: 2Gi
  className: local-path

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 4Gi

auth:
  enabled: true
  aclUsers:
    default:
      permissions: "~* &* +@all"
      password: "CHANGE_ME"                # ← 务必替换
  replicationUser: "default"

podDisruptionBudget:
  enabled: false
  maxUnavailable: 1
